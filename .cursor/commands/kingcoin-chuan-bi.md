---
description: KingCoin — chuẩn bị môi trường dev (Docker, DB, API, web)
---

Bạn đang trong monorepo **KingCoin**. Thực hiện checklist **«chuẩn bị»** trong `.cursor/skills/kingcoin-workspace/SKILL.md`: `npm run dev:all` (hoặc `dev-up` rồi `npm run start:all`) → `curl` health + `token-crypto/all`. Cuối cùng báo URL web và API.
