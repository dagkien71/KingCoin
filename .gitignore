# Graphify — local-only artifacts (commit graphify-out/ if team shares the map)
graphify-out/manifest.json
graphify-out/cost.json

# Dependencies
node_modules/

# Env & secrets
.env
.env.*
!.env.example
!frontend/.env.example
!frontend/.env.production

# OS
.DS_Store

# Upload local (dev fallback)
backend/uploads/
