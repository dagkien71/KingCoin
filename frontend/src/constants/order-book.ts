/** Số mức giá tối đa mỗi bên (1 “trang” sổ lệnh). */
export const ORDER_BOOK_LEVELS_PER_SIDE = 10;

/** Số chữ số thập phân — giá / tổng KC trên sổ lệnh & giá tham chiếu giữa. */
export const ORDER_BOOK_PRICE_DECIMALS = 4;

/** Chiều cao một dòng (px). */
export const ORDER_BOOK_ROW_HEIGHT_PX = 22;

/** Chiều cao cột sổ lệnh trên trang trade (gồm tab + bán + giữa + mua). */
export const ORDER_BOOK_PANEL_HEIGHT_PX = 620;
